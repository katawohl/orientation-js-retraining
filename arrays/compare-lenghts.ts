let firstList: number[] = [1, 2, 3];
let secondList: number[] = [4, 5];

if (secondList.length > firstList.length){
    console.log("The second list is longer")
} else {
    console.log("The first list is longer (or equal to the second one)")
}